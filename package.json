{
  "name": "indeklimaguiden",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "prisma migrate deploy && astro dev",
    "build": "astro build && pagefind --site dist/client",
    "build:astro": "astro build",
    "db:dev": "prisma migrate dev",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "postinstall": "prisma generate",
    "preview": "prisma migrate deploy && astro preview",
    "start": "prisma migrate deploy && node ./dist/server/entry.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/mdx": "^7.0.0",
    "@astrojs/node": "^11.0.1",
    "@astrojs/sitemap": "^3.7.3",
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "astro": "^7.0.2",
    "dotenv": "^17.4.2",
    "pagefind": "^1.5.2"
  },
  "devDependencies": {
    "prisma": "^7.8.0",
    "tsx": "^4.22.4"
  }
}
